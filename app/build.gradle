apply plugin: 'com.android.application'

android {
    signingConfigs {
        lzp {
            keyAlias 'aizixing'
            keyPassword 'lzp123456'
            storeFile file('E:/破解成功、/lzp.keystore')
            storePassword 'lzp123456'
        }
    }
    compileSdkVersion 26
    buildToolsVersion "26.0.0"
    defaultConfig {
        applicationId "com.example.tuifangapp"
        minSdkVersion 19
        targetSdkVersion 26
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
            signingConfig signingConfigs.lzp
        }
    }
}

dependencies {
    compile files('libs/ksoap2-android-assembly-3.6.2-jar-with-dependencies.jar')
    compile 'com.android.support:appcompat-v7:26.0.0-beta1'
    compile 'com.android.support:support-v4:26.0.0-alpha1'
}
